<html>
  <head>
  </head>
  <body>
    <div class="pageContainer">
        <% if (locals.file) { %>
        <div>
          <div>
              <%= file.name %>
          </div>
          <div>
              <%= file.size %> Bytes
          </div>
          <div>
              Uploaded at:<%= file.upload_time %>
          </div>
          <a target="_blank" href="<%= file.url %>"><button >Download</button></a>
          <% if (locals.owner) { %>
          <div>
            <form action=<%= `/storage/delete/${file.id}` %> method="POST">
                <button type="submit">Delete</button>
            </form>
          </div>
          <% } %>
        </div>
    <% } %>
    </div>
  </body>
</html>