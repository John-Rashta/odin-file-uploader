<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
  </head>
  <body>
    <div class="pageContainer">
        <% if (locals.file) { %>
        <div>
          <div>
              <%= file.name %>
          </div>
          <div>
              <%= file.size %> Bytes
          </div>
          <div>
              Uploaded at:<%= file.upload_time %>
          </div>
          <a target="_blank" href="<%= file.url %>" download><button >Download</button></a>
          <% if (locals.owner) { %>
          <div>
            <form action=<%= `/storage/delete/${file.id}` %> method="POST">
                <button type="submit">Delete</button>
            </form>
          </div>
          <% } %>
        </div>
    <% } %>
    </div>
  </body>
</html>